# Core
anthropic>=0.40.0

# API server (optional — only needed for api/server.py)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# API client (optional — only needed for api/client.py)
requests>=2.31.0